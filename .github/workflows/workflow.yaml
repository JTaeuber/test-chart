name: Test Workflow

on:
  push:

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get the Git Tag
        run: 'echo "Git tag: ${GITHUB_REF##*/}"'
